# Gameplay and feature parity matrix

Last updated: 2026-09-23

Status values are defined in [IMPLEMENTATION-PLAN.md](IMPLEMENTATION-PLAN.md#8-completion-tracking-conventions).
`Manual` confidence means the intended behavior has been inspected but not yet
confirmed in the binary. Rows are grouped by area; every row keeps the same
five columns, and a row's finding and rule IDs resolve through
[ORIGINAL-INTERNALS.md](ORIGINAL-INTERNALS.md#finding-index) and
[GAME-RULES.md](GAME-RULES.md#rule-index). The topic index in
[README.md](README.md#topic-index) lists which rows cover which subsystem.

<!-- doc-index:begin toc depth=2 -->
- [Source data and extraction](#source-data-and-extraction)
- [Turn structure and match state](#turn-structure-and-match-state)
- [Commands and economy](#commands-and-economy)
- [Combat and police](#combat-and-police)
- [Objectives, timers, and endgame](#objectives-timers-and-endgame)
- [Computer players](#computer-players)
- [Persistence](#persistence)
- [Interface, input, and Options](#interface-input-and-options)
- [Help and media](#help-and-media)
- [Multiple players and networking](#multiple-players-and-networking)
- [Computer-player row details](#computer-player-row-details)
- [Current blockers to parity claims](#current-blockers-to-parity-claims)
- [Binary evidence status](#binary-evidence-status)
<!-- doc-index:end -->

## Source data and extraction

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| Source pack | Exact supported DATA/HELP/MUSIC set | Implemented | Full SHA-256, Verified | Add second-source-version test |
| Output pack | 686 outputs from 471 original resources | Implemented | Exact manifest-to-disk inventory, transactional whole-directory promotion, per-file size/SHA-256, and schema-v1 JSON quick/full reports with categorized diagnostics, including one locally decoded help derivative; Verified | Add a second supported source-version fixture |
| PX16 | Repair four missing BMP fields as RGB555 | Implemented; native loader/header repair, palette handoff, opaque/stretch copies, pattern mode, and exact-white keyed roles recovered; inferred black alpha removed; mixed-copy `PX00129` split into opaque/keyed atlases, while the exact embedded resource-146 stencil is retained as evidence for the now-classified unsupported `PX00146` participant screen rather than misapplied to scaled local-setup art | Payload arithmetic plus 12,065,806 paired-pixel comparison and complete copy-wrapper source attribution/dataflow, High | Name remaining unused `PX00129` rectangles; pixel-compare the 16-bit white-key boundary in native captures |
| PX08 | 207 RLE8 and 7 uncompressed indexed graphics | Implemented | Strict decoder, full 214-file extraction and payload checks; High | Compare palettes/pixels with PX16 and reference rendering |
| Tables | 22 sites, 90 gangs, 64 items | Parity verified (values only) | Source/payload hashes, Verified | Semantic field-use fixtures |

## Turn structure and match state

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| Turn phases | Upkeep, Command, Execution, Hire, Elimination | Headless coordinator implemented with explicit deterministic boundaries around the native monolithic turn loop | Manual phase names High; static outer loop and whole-turn resolver High for ascending fixed player-slot planning, inactive-slot skips, and deferred resolution | Runtime presentation-boundary corroboration |
| Execution phases | Instant, Combat, Transaction, Chaos, Movement, Control | Headless coordinator implemented with the recovered physical split: Chaos rolls/Crackdown creation after Instant and before Combat, delayed Chaos payout after Transactions, then Terminate, Move, and Control | Static whole-turn resolver pass ordering and all per-phase schedulers High; public boundaries Manual, High | Runtime corroboration |
| Action IDs | None plus 14 original commands | Data-driven validation, typed queue, and headless resolution implemented for every command | Table/save notes and manual plus static assignment, terminal-switch, and whole-turn resolver inventories High for IDs, phase membership, and native schedulers; action-specific confidence is recorded below | Runtime corroboration for the explicitly listed message/presentation edges |
| City | 8x8 sectors, three sites each | Recovered 32x32 density field, exact income/tolerance rounding, uniform 0..20 proposal draws, staged duplicate/stat-balance rejection, and Armageddon exclusions | Static executable control flow and accepted-setup-to-city RNG order, High; fixed recreation seed vectors protect call order, but original runtime fixture remains pending | Capture an original initial-city fixture and its process RNG context |
| Players | One-to-six local humans; six participating slots after omitted local slots become Computers on Begin | Setup starts with Kill 'Em All selected, one human, and player card zero selected; Add selects the new slot, Remove deletes the selected slot and selects the highest survivor, a first click selects another card before its arrow/name actions work, selectable portraits exclude empty marker 15, and face drag moves into an empty color or exchanges two human colors while selecting the destination; fresh-match factory normalizes sparse human slots and completes omissions in ascending order with bounded unique portrait 0..14 draws, resource names, Computer controllers, and pre-AI/city RNG ordering | Five identical frames from the first bounded original-runtime capture verify the fresh Kill 'Em All selection; complete local setup handler/renderer/drag helper High; fixed contiguous/sparse recreation vectors, remaining runtime fixture fields pending | Sanitize the captured setup observation and finish exact setup alignment |
| Gang capacity | 80 usable hire slots per player, six friendly per sector | Implemented | Static hire resolver proves ascending reuse and a strict first-80-slot bound despite 81-record native storage; static Move prepass proves the six-gang projected-count bound, High | Runtime boundary corroboration |
| Gang identity | Stable gang instance identity, owner, force, sector, equipment and command projection | Headless schema implemented | Recreation invariant, not an original-format claim | Correlate IDs with save slots |
| Command queue | One action per gang, replace/cancel/repeat | Structured validation, typed mutation results, ordered queue events, exact replacement, and terminal repeat cleanup implemented; the individual recurring picker and core validation permit only Chaos, Control, Heal, Hide, Influence, and Research. Bribe and Snitch are one-off, as are Attack, Equip, Give, Move, Sell, and Terminate. The recovered sector-wide recurring menu also omits Research. The turn-start scan clears Control on ownership/Crackdown, Heal at Force 10, Influence on completion/ownership loss, Research on completion, and every inactive gang before copying retained action/target fields. | Exhaustive recurring-field reference inventory, both assignment handlers, active/recurring target writes, terminal switch, scan/copy/update order, and inactive sentinel High; manual supports queued/repeat actions | Native UI corroboration |
| Determinism state | Explicit RNG state/consumption and phase hashes | Recovered VS98 raw step, exact below-one clamp plus three-sample range wrapper, once-per-process `timeGetTime() & 0xffff` local seed derivation, and exact accepted-local-Begin ordering through omitted portraits, AI reactions, city generation, and HQ permutation; all 61 direct bounded-wrapper calls in 24 functions are classified; recreation deliberately excludes the original registry loader's conditional two bounded `serialNum` draws and failed read-only-handle write; serializable state and canonical SHA-256 encoding | Static binary addresses/constants, unique seed writer/caller, exhaustive wrapper reference inventory, registry load/write masks, and bounded setup/AI/resolver call graphs High; clock-derived state and native-stream correlation require a runtime fixture | Validate initial city/rolls/phase hashes against a native launch with captured seed and effective `prefsFullScreen`/`serialNum` query state |
| Notifications | Bounded per-player ordered queues | Presentation-independent mechanical notification schema plus an exact native Last Turn Events projection: only the ten recovered report types from the immediately completed turn, original status strings, and first-32 keep-first capacity; automatic handoff review starts on page one, records every visited page, clears through replay-recorded dismissals only after all pages were seen, and otherwise preserves the current-turn view plus blinking Events indicator | Static table/recorder/reset/caller/compositor/string resources High for kind filtering, capacity, order, and resolution lifetime; Help unread-close behavior High; exact handoff presentation timing Medium | Native handoff presentation corroboration |
| Comlink | View/send messages among human Overlords; newest 16 received messages retained | Authoritative active-human send validation, deterministic multi-recipient delivery, exact bounded inboxes, per-record unread state, v22 persistence, v25 hashing, and v24 replay; original `PX05017` View and `PX05018` Send panels are routed with paging, unread indication, six recipient cells, and four 40-character composition rows. View now starts at the first unread retained record and uses its recovered previous/next/dismiss hit boxes and Left/Right/Enter/Execute navigation; Send uses recovered recipient hit boxes, Execute submission, and a fixed 4x40 overwrite cursor with native key/wrap/clamp behavior and glyph grid placement. | Manual workflow, original panel assets, complete native record layout/projection, View/Send handlers `0x0045d61a`/`0x0045e04d`/`0x0045eab1`, and caret/cell helpers `0x0046023c`/`0x004600d2` High; legacy transport and Send inverse-caret cadence remain Medium | Native interactive/golden-screen validation and Send inverse-caret compositor/cadence |
| Starting state | Right Hands in controlled Headquarters sector; seven zero-difficulty technologies start researched; Armageddon starts with $500 and all items researched | Explicit-layout bootstrap plus recovered six-candidate HQ permutation, Force-10 Right Hands, $20 ordinary cash, exact ordinary item-difficulty and Armageddon all-zero Research initialization, exact `SMGFUNDAGE` $1,500 override; `SMGISLANDS` applies permanent Crackdown duration 100 to neutral non-HQ sectors; exact `SMGSPANK` six-Right-Hands opening, exact `SMGKICKASS` five-extra equipped GROUND ZERO opening, exact `SMGHUBBLE` global opposing-gang visibility, and deferred fixed hire slots | Static executable setup, Research-array initialization, duration-field consumers, and visibility paths High; fixed recreation vectors, runtime fixture pending | Capture an original initial-state fixture |

## Commands and economy

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| Hire pool | Three fixed offers; one mutually exclusive hire-or-snub action; same-slot replacement next planning turn; new gangs start at 5–9 Force | Fixed authoritative slots, exact replacement/retarget/Reject toggle state machine, deferred resolution-time payment, recovered failure/RNG order, in-place tombstones, delayed ascending-slot refill before handoff reports, immediate replacement portrait projection, rejection sampling, exact `SMGMILK` Force-10/no-RNG modifier, UI projection, hashes, saves and replays implemented | Manual page 17 plus executable arrays/resolver/refill/UI/name-cheat paths, High | Runtime corroboration |
| Cash/upkeep | Setup cash is unchanged before initial planning; each later controlled sector grants $1 plus influenced-site Cash; active-gang upkeep is subtracted; negative balances restrict purchases, Bribe, and paid hires, while shipped Snitch remains free | Guarded first-turn skip, ordered later Upkeep resolver, and City/Sector Finance projection expose the original combined sector byte as separate tax/site components, persist debt, and implement runtime command restrictions, debt-independent Snitch, zero-cost hire exception, events, notifications and hashes; Cash Earned/Spent classifies every signed gang Upkeep and combined sector Income independently before components can cancel | Static cash-updater/recomputation/statistics arrays, exact sign branches, offsets, initial skip, and order High; manual restrictions and static Snitch exception High | Reference debt and multi-turn fixtures |
| Sector control | Grouped Force + Control minus generated sector Income, defenders and site Support; sectors resolve in board order; zero margin includes a neutral/no-capture candidate; tied best players are selected in ascending player-slot order; empty sectors retain ownership; overthrow resets influence | Board-ordered phase-opening owner/defense conflict resolver for neutral and controlled sectors, player/roster aggregation, generated-Income defense, recovered positive- and zero-margin bounded tie selection with recorded roll/range, execution-time Crackdown lockout, retained ownership after last-gang Move/Terminate, single-winner ownership/site reset, Support and Overthrow accounting implemented; deliberately fixes the shipped negative-defense bug that can award ownership to a player who issued no Control command | Manual equation and single-player zero chance High; static sector/player/roster scans, winner initialization, candidate list, RNG call, nonparticipant defect, same-turn Crackdown exclusion, capture predicate and exhaustive whole-turn owner writes High; recreation ordering and Crackdown integration High | Obtain runtime tie corroboration |
| Influence | Cooperative Force + Influence versus site resistance; ownership and local benefits after the following pre-planning rebuild | Binary-ordered per-gang Instant rolls, cumulative persistent resistance, post-completion roll suppression, pending completion, next-planning ownership/Support/Tolerance/stat activation, completed-site command rejection, capture-time cooperation reset, native-save/replay continuation, notifications and hashes implemented; the original has no separate site-takeover action because control changes zero all site progress before the new owner can reinfluence. `PX05005` uses recovered individual site click targets and guarded double-click detail entry. | Manual base formula/local scope High; static action switch, roster scan, progress mutation, picker eligibility, sector-record layout, ownership-change reset, site-field reference inventory, rebuild call order, and handler `0x0043f692` target/detail branches High | Runtime same-site and post-overthrow reinfluence corroboration |
| Chaos | Force + Chaos + generated sector Income for each participating gang; fixed player/roster-slot roll order; temporary player-sector aggregation before uncontrolled half payout; sector-wide crackdown suppression; post-Instant tolerance is at least 1; no persistent or displayed sector-Chaos field | Pre-Combat per-gang roll/Crackdown preparation, resolver-local sector totals, post-Transaction grouped payout, cash/statistics, post-Instant tolerance floor and broadcast notifications implemented; generated Income drives the pool, while the owner-only Cash row remains tax plus influenced-site Cash | Static pass/roll/temporary-storage/aggregation/ownership/division order, independent Income/Cash field reads, renderer rows, and tolerance floor High; recreation ordering/save/RNG fixtures High | Run controlled original experiments at tolerance boundaries and police aftermath |
| Bribe | Manual prints $5/+3/max 40; executable uses $3/+3 without a resolver clamp | Binary-compatible Instant threshold, direct tolerance mutation, cash statistic, ordered success/failure result, finance projection, and AI budgeting implemented; printed helper remains isolated | Static action block High for threshold/cost/statistic/tolerance delta and failure branch; Last Turn compositor proves exact `INSUFFICIENT CASH TO BRIBE.` wording; recurring-menu inventory proves Bribe is one-off | Runtime corroboration |
| Snitch/tolerance | Free and debt-independent; direct -3 followed by a global post-Instant minimum of 1; Upkeep moves one point toward income/site-modified normal | Binary-compatible direct Instant delta and phase floor, next-planning site modifiers, and deterministic Upkeep normalization implemented | Static Snitch block, global sector clamp, and site rebuild High; manual printed zero floor differs | Visible intermediate-result fixture and influence-loss boundary |
| Heal | Base four dice plus effective Heal, success restores force to max 10 | Instant-phase resolver with deterministic rolls/results implemented; effective Heal includes gang, all three equipped-item, and controlled completed-site modifiers; assignment at full Force is blocked and a repeating order clears at maximum | Static pool, effective-stat rebuild, ownership scope, thresholds, Force cap, and repeat cleanup High; recovered RNG algorithm High; seed/context Low | Original-runtime RNG-order fixture |
| Hide/detect | Active action 8 is hidden immediately; one-off Hide expires when recurring None is copied at next Upkeep, recurring Hide remains active until replaced/cancelled; individual attack evasion and cooperative sector visibility | Exact active/recurring lifecycle, recovered d20 evasion threshold (`Stealth + 14 - Detect` for bands 0/1, `Stealth + 10 - Detect` for band 2), no retaliation on an evaded hit, and executable cooperative visibility aggregation: strongest effective Detect plus at least one per additional active gang, with uncapped two-point helper bands from Detect 10; native picker and authoritative validation both exclude undetected Attack targets | Static action-field writes, turn-start copy, combat/police consumers, complete visibility rebuild, Attack roster-builder gate, effective-stat offsets, and formulas High; native UI fixture pending | Native targetability presentation fixture |
| Movement | Eight-neighbor (including diagonal) sector move with six-friendly-gang capacity; all Terminate commands precede Move | Separate binary-ordered Terminate and Move passes; native simultaneous per-player destination normalization repairs the highest overcrowded sector repeatedly, cancels the earliest eligible roster destination to its source as a successful no-op, and preserves later roster priority; literal mode-0 all-sector fallback and drag-to-neighbor planning implemented. The `PX05006` Move panel uses its recovered 3-by-3 54-by-52 neighborhood cells, explicitly leaving the center cell inert. | Manual High for adjacency; static whole-turn resolver and complete `0x00476a94` helper High for pass precedence, counts, repair order, six-gang boundary, target rewrites, and fallback RNG; Move handler `0x004413ef` geometry/mapping High | Runtime corroboration |
| Equipment | One weapon/armor/misc; separate melee/ranged browsing; research/tech gates; fixed player/roster-slot transaction scan; deferred incoming gifts; replacement loss; single Sell credit after fixed-slot mask processing | Equip/Give/Sell Transaction resolvers, typed slots, cash/statistics, validation, events and notifications implemented; `PX05004` Equip uses recovered inset category cells and a fixed sixteen-row, nine-pixel list, while `PX05007` Research retains that cadence with its separately recovered taller-upward list hit box. `PX05013` Sell and `PX05015` Give use recovered item-selection cells. Give selects one of up to five eligible same-panel recipients through recovered cells, while retained Up/Down recipient cycling is a documented QoL addition; grouped Give resolution supports swaps and post-transaction recipient overwrite; multi-slot Sell preserves the binary's highest-selected-slot-only payout; a controlled-sector active Factory applies `Cost - trunc(Cost / 3)`, while one completed during Instant waits until next planning; state validation rejects impossible site ownership | Static transaction loop/action/pending-array/Sell-local, Factory branch/divisor/rounding, Equip `0x0043dad9` and Research `0x004427fa` category/list geometry, Give `0x00445a4f` and Sell `0x00443bbd` selection targets/confirmation predicates, and site rebuild High; manual formulas/site-control prerequisite and decoded categories High; recreation ordering, deferred replacement, all-cost, ownership-invariant, and delayed-acquisition fixtures High | Native Equip/Research/Give/Sell interaction golden screens and repeat-command fixture |
| Research | Force + effective Research dice, persistent progress/completion; seven zero-difficulty items initially complete; gang tech and base-5/Science-8/Lab-10 site caps | Player/roster-ordered Instant resolver with deterministic rolls, immediate progress/completion state and suppression of later same-item rolls after completion; exact base/all-equipment/completed-site statistic aggregation, item-difficulty/Armageddon initialization, validation, gang/site cap enforcement, result notification and hashes implemented; controlled-sector influenced research buildings raise capacity, state validation enforces completed ownership, and phase-opening effective-stat snapshots prevent a newly influenced Science Center or Research Lab from changing a concurrent Research pool | Static effective-stat rebuild, initializer array/field/branch, resolver pool/guard/order, repeat cleanup, and manual caps High; site-control prerequisite and simultaneous phase statement High; recreation snapshot/ownership integration High; seed/context Low | Original-runtime cap timing, unlock, and RNG-order fixtures |

## Combat and police

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| Combat | Force + class-modified Combat − Defense dice; simultaneous damage and halved retaliation; Martial Arts exception | Player/roster-ordered phase-wide snapshot resolver coalesces reciprocal orders into one opening attack and one retaliation; recovered 0/1/2 bands drive defender Defense reduction, 6+/5+/4+ opening thresholds, positive-pool minimum damage, exact Hide/weapon/Martial-Arts retaliation eligibility, and 5+/5+/4+ halved retaliation; every opening attack credits full computed Damage Inflicted even beyond remaining Force, while retaliation credits none; police rolls use a subsequent player/roster pass; combat/police death and Terminate retain inaccessible equipment on the inactive record; statistics, events and notifications implemented | Static binary formulas, statistic write, retaliation gate, action lifecycle, death/Terminate record writes, and both scan orders High; supplied reciprocal-combat observation Medium | Fixed runtime formula/RNG matrix and targetability fixtures |
| Police | Executable detection is `clamp(115 − 5×Stealth − (Hide ? 20 : 0), 0, 100)`; detected attacks roll `max(0, Force 5 + Combat 20 − Defense)` dice at 5+; additive 3–5 duration; two occurrence slots use an inclusive five-turn boundary and reset both to the current turn on neutralization; Control prohibited while present | Deterministic player/roster-ordered post-attack rolls with exact visible/Hide detection and attack pools, pre-Combat creation for a same-turn attack, common post-attack duration decrement leaving 2–4 future phases, duration extension, exact two-slot occurrence mutation with v4 save migration, control/influence cleanup, displaced-owner notification, visible countdown, damage, events, notifications, casualties, and submission/runtime Control lockout implemented | Static player/roster scan, detection/attack/decrement call sites, expiration/fill/reset/cleanup/duration/RNG order and duration/history/lockout High; message wording Low | Native notification fixtures |

## Objectives, timers, and endgame

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| Player elimination | Remove players with neither a sector nor an active gang; Eliminate removes a player with no living Right Hands | End-of-turn status transition, exact Eliminate sector/site reset, preserved retired equipment, player/recipient-ordered reports, events and notifications implemented; Force-zero/queue cleanup represents native sector-100 inactivity | Manual plus unique active-byte writer, sole cleanup call, complete helper and report-loop static analysis, High for triggers, effects, cleanup, and ordering | Runtime multi-player elimination corroboration |
| Objectives | Ten named scenarios and objective thresholds | Binary-ordered end-turn evaluation and exact all-scenario score/competition-standing projection, including persistent Big Man points rather than current-center rescoring; exactly one active Overlord ends every scenario early, while hot-seat continues after an elimination only with multiple survivors; fresh Siege matches designate all six starting controlled HQ sectors important; the city uses the exact white-keyed native pylon overlay on those landmarks and Big Man's four center sectors; Big Man center-sector point accrual, Eliminate cleanup, tied winners, events/notifications and outcome hashing | Static evaluator/scorer/presentation paths and manual pages 12–15 High for cleanup-before-objective timing, survivor rule, thresholds, weights, scores, ties, single-player defeat, Siege mapping, and exact Siege/Big Man pylon art/placement; remaining special presentation edges Low | Special-edge fixtures and objective golden screens |
| Timers | Scenario limits of 26/52/104/208 turns; optional human planning limits of None/30 seconds/2 minutes/5 minutes | Scenario limits are authoritative model state. The recovered enabled-per-match planning countdown uses its original setup hit regions and percent-first quantized 60-pixel main-panel bar, runs through planning panels, checks slots 7/8 every sixth fixed update in the last 10/final second, and submits the normal replay-recorded finish-planning operation on expiry without showing the idle-gang confirmation. | Help and static executable mapping/expiry/bar/sound/update-divider boundaries High; wall-clock cadence and modal behavior Medium | Controlled original wall-clock warning/deactivation/modal capture and end-turn boundary fixtures |
| Endgame | Victory/elimination result, ranking, five awards, statistics | A completed single-player victory first renders the configured human Overlord on mapped `PX00202`, then advances to `PX00200`; single-player defeat renders `PX00203` and returns to title; the six standing rows use recovered all-scenario scores, competition places, player-slot tie order, and trailing unranked eliminated players, then switch between mapped `PX00201` award icons and five statistics; native award priority is Fist, Skull, Chicken, Dollar, Safe with inclusive 5/50/10 activity thresholds, all player slots and ties eligible, and the first three awards visible per row; every resolved Hide counts, so recurring Hide counts again after the next Upkeep; a completed hot-seat game correctly goes directly to shared standings, while an ongoing local elimination follows the native slot walk as `PX00132` handoff then private `PX00203` before later seats; outcome events and hashes retain every award; objective predicates exclude eliminated players and preserve simultaneous active winners | Static standing/award-table consumers, final single/multi-human branch, terminal-player caller, thresholds, scan order, unconditional Hide-counter increment, three-icon loop, and visible result/icon assets High; Help/manual pages 46–47 High for defeat flow; exact row typography/timing Medium-Low | Capture mid-match hot-seat geometry and golden screens |

## Computer players

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| AI | Objective/difficulty-aware computer players | Deterministic objective-aware planner with recovered reaction/attitude initialization and updates, all nine difficulty-band consumers, role/family assignment, action history, live-equivalent offer refills, hiring and placement, every dispatcher family handler, and all family-1 continuations. Six-by-81 polymorphic family-2/7 focus/family-11 formation values and family-6 coverage sectors preserve inactive family records and are hashed/saved/replayed. Family 2 uses exact ten-scenario competition standings for mode-6 leader routing. No recovered dispatcher family falls through to provisional scoring; recovered dispatch, shared operations, and recreation-native fallback scoring have separate partials. The shipped planner's verified slot-versus-role indexing defect in five family-6 hire guards is deliberately corrected: every scenario tests prior role 4 instead of an unrelated scenario-specific slot number. Six-computer fixtures cover every scenario through 20 turns, every timed scenario to completion, and every objective scenario through replay-verified 40-turn campaigns across seven guarded seeds which must hire, expand, and meet scenario-specific combat/control activity guards; Big Man completes naturally by turn 60 at one guarded seed. Neutral all-computer Criminal Kill 'Em All runs may remain non-hostile under the recovered attitude rule, while any surviving hostility must produce attack activity. Eliminated planning slots are skipped through recorded transitions. Tournament diagnostics identify the live scenario/seed and report ten-turn boundary/event/elapsed checkpoints under a 30-minute runner timeout. | Strategic-state, family-handler, persistence, resolution-band integration, and bounded six-player durability High; selector `0x8f` storage and the documented bug boundary High static evidence; original-runtime fixture breadth and complete outer planning parity Low | Add fixed original-runtime family traces, expand multi-seed tournaments toward the release-scale matrix, and add evidence-led completion policy for Kill 'Em All, Big 40, Eliminate, Siege, and Armageddon |

## Persistence

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| Original saves | Standalone `S40W` full-state envelope, legacy-network `N40W` extension, and accepted `M10W` auxiliary stub | Explicit non-goal: no import or export support; unsafe partial-read mutation is deliberately not reproduced | Complete static read/write inventory fixes the two full-state sizes at 45,305 and 45,329 bytes, proves the six-DWORD network delta and repeated-marker trailer, and bounds the stub at 16 bytes; High for the envelope, deliberate product boundary for interoperability | None; exclusion is final |
| Native saves | Versioned safe recreation format | Version 24 snapshots preserve the authenticated Original/Advanced AI policy and all authoritative runtime state while omitting obsolete synthetic sector Chaos; canonical hash v27 authenticates the same state boundary. Version 23/hash v26 remains available as a legacy verification projection. Notification restore rejects future turns, mismatched referenced-event boundaries, and unknown gangs before hash acceptance; deterministic encoding, bounds, atomic replacement, backup-only slot discovery, validated self-healing primary recovery, F5/F9 quick saves, autosaves, and reusable migration machinery are implemented | Recreation format; deterministic continuation and migrations from versions 1-23, including preserved legacy hashes, High; pre-1.0 compatibility is not guaranteed | Keep current-format safety and reusable migration machinery; enforce migration or documented rejection after 1.0.0 |
| Replays | Deterministic recreation playback | Version 28 records a v24 initial snapshot plus every public match mutation, including controller transfers, the authenticated AI policy, per-message Comlink send/read state, individual and simultaneous hire-offer preparation, AI planning/hiring preparation, validation result, and resulting v27 state hash. The bounded loader rejects divergence, out-of-band mutation, and operations or payloads mislabeled with a schema version predating their introduction; full-turn/client F6/F10 record/playback and development-schema migration coverage are implemented. | Recreation format; current full-turn and delayed-site deterministic, operation-version, event-integrity, and file-store tests, High; pre-1.0 compatibility is not guaranteed | Animated playback controls and post-1.0 compatibility policy enforcement |

## Interface, input, and Options

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| City/Sector UI | Original screen/panel workflow | Routed title/setup/handoff/city/sector/gang/finance/ranking/items/Give/Sell/combat-summary/search/commands/hire/events/endgame/help flow starts authoritative recovered cities; setup top portraits and two-by-three player cards use the measured `PX00143` apertures and `PX00140` arrow/name construction, while selection outlines use inset button faces rather than overlapping broad hit rows; queued reports automatically open the paged `PX05010` Last Turn Events panel after handoff using native `PX060xx` illustrations, with completed Research compositing the item's 15-frame `PX04xxx` rotation inside the exact `PX06005` monitor aperture, and Combat Summary uses paged `PX05012` results with native-size sector tiles; Search uses the original `PX05024` site checklist with all 22 per-player filter bytes, exact ALL/NONE/site row targets, exact `PX00150` controlled/selected site markers compacted into three sector slots, and definition-detail double-clicks instead of the former incorrect gang list and sector outlines; the city retains one neutral grid and composites owned-cell interiors without border drift, while whole-city and detailed-sector maps both mark every active-gang sector with exact-white-keyed `PX00129` status art; the upper-right city console shows projected cashflow and hover explanations that distinguish sector Income from passive tax; the shared top bar uses measured portrait cells and the twelve-frame `PX00129` active-player marker; the detailed-sector screen preserves the portrait strip, live right-console routes, 3-by-3 neighborhood, coordinate frames, status overlays and three buildings; the detailed-sector portrait strip additionally flags each opponent holding detected gangs in the selected sector with a red `GANGS` banner and lends the gang cards to that opponent's detected roster until the viewer's own portrait, a sector change, or a fresh entry restores their own gangs; Gang Information slides over a stationary City/Sector backdrop, command selection does not slide, nested-detail return does not replay the Sector entrance, disabling Slide Panels cancels every entrance globally, and screen changes clear stale detail-click state; `PX05021`, City Financial, Comlink View, Gangs in Sector, and the three-column `PX05016` Hire comparison use their embedded baselines/apertures and shared panel destination; recreation-only navigation/drag/success hints are suppressed while rejection reasons remain; gang cards have assigned-action strips, stacked repeat marks and green/red Force tracks, stationary double-click opens details, and drag-to-neighbor queues Move; Hire uses the original split price/reject footers and opens `PX05000`; Equip/Research use four category tabs on `PX05004`/`PX05007`, show accumulated/required Research progress, and share `PX05001` Item Information with its exact 48-by-48 aperture and continuous 15-frame item rotation; `PX05002` shows site details from sector/Influence or Search definitions; shared command portraits and Site/Attack subcells use measured template apertures; Influence uses `PX05005`; Attack uses the recovered `PX05003` opponent and target hit maps; `PX05013` and `PX05015` provide one-to-three-slot Sell and Give workflows; resolved combat uses the original `PX05014` sector/combatant/equipment/Force layout with recovered animation frames; all mutations are replay-recorded | Static executable evidence High for generator, mapped atlas identities and copy modes, `PX04xxx` dimensions, recovered panel pixels, complete Search handler/targets, Attack handler `0x0043b290`, and Search filter/marker/detail behavior; supplied panel captures High for workflow and Medium for remaining exact hit edges; remaining interaction/coordinates Medium-Low | Add Search and city golden screens |
| Input | Original mouse plus modern keyboard | Letterboxed virtual-coordinate mouse hit testing and keyboard navigation implemented; the recovered Add/Remove/Begin/Cancel setup rectangles retain their pressed identity, draw the exact `PX00140` held-inside tile, restore the baked control outside, act only on release inside the same control, and cancel release outside; setup cards use the exact native selected-card-first gate, 64-by-68 input cells, broad side-arrow and ten-pixel name bands, four-pixel drag box, clamped 40-by-40 token with its keyed native frame, destination selection, and whole-record color move/exchange while retaining separate artwork rectangles; the main city/sector console uses the exact eight native tiles, horizontal paired subcontrols, original-press identity, release-inside cancellation, and opaque `PX00129` pressed sprites; names share a bounded original-font keyboard mapper and enforce the original 10-character cap; city sectors select on single-click and open detail on bounded same-sector double-click; Hire portraits drag as scaled tokens through controlled-sector validation from both city and detailed-sector modes, with a detailed-workspace drop targeting its centered sector; detailed gang portraits use movement-threshold disambiguation so stationary double-click opens details while drag/drop queues only validator-legal neighboring Move targets; a right-button press edge cancels transient edits, pressed controls, drags, warnings, Detailed Combat playback, and nested panels through their existing close/back paths without held-button repeats; Done presents the recovered optional confirmation when an active gang has no queued command | Complete local-setup and main-console handler/helper paths High; current client and original Hire/sector captures Medium-High; idle-gang Help/static paths High; right-click behavior is recreation integration pending exact original per-screen captures | Remaining original hit maps, exact original per-screen right-click captures, and configurable bindings |
| Pointer cursor | System cursor ownership | Framework-managed standard pointer; no custom `PX` cursor is drawn | Original cursor helper/import census High: all 35 direct callers select stock Arrow or Wait; I-beam, Cross, and No remain uncalled branches | Native screenshot corroboration only |
| Options | Persistent audiovisual and presentation choices | The recreation defaults to Current gang statistics, Detailed Combat on, Slide Panels off, Warn If Idle Gangs on, Original Event Site Images, and Original AI for future new matches. The disabled panel-motion default is an intentional modern usability choice; the recovered original value was on (`prefsSlide=1`). Its safe versioned local persistence deliberately corrects the executable's read-only-HKLM writer and stale shared query buffer. Preference version 9 adds the Options-only Advanced AI default; it initializes future new matches but never changes an active or loaded match's authenticated policy. Recreation-native F11/Alt+Enter windowed/borderless-fullscreen switching is global and persistent and does not alter compatibility coordinates. Event Site Images defaults to the recovered centered crop, `COLORONCOLOR`-equivalent stretch, and resource-146 25-percent ordered mask, with an explicit unmasked linear-filtered Smooth alternative. Panel entrances follow the recovered right-to-left 344-pixel primary or 320-pixel alternate travel and quarter-second benchmark target while leaving the underlying view stationary when enabled. The panel slide-out is deliberately not animated ([decision](DECISIONS.md#2026-09-23--do-not-animate-the-panel-slide-out)), so a cue it once separated can cut off the cue before it. The alternate form is assigned exactly to Item/Site Information, City/Sector Financial, Hire comparison, Game Info, and Gang Definition Information. | Initialized bytes, complete ordered registry load/save calls, access masks and failure behavior, exact preference mapping, rectangle/blit field order, complete literal caller/resource classification, and panel copy/cal functions High for recovered defaults/direction/travel forms/target duration; original event-image crop/stretch/mask is High static evidence; display, Advanced AI, smooth filtering, safe persistence, and the disabled slide default are intentional modern choices; runtime cadence Medium-Low | Capture Options open/close and adjacent-panel cadence |

## Help and media

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| Help | Original WinHelp topics and contextual Help command | Asset-pack format 6 decodes the supported local `Chaos.hlp`/`CHAOS.CNT` pair into a versioned 80-record document, preserves all 80 native context hashes/target offsets, nine legacy font descriptors as 779 normalized styled runs, all 59 contents symbols, and the empty numeric `|CTXOMAP`. F1 navigates the 59 player-facing entries in original order, omits 21 unlisted fragments from the index, uses exact screen-context symbols, and makes all 67 topic jumps plus 26 popup links clickable; popup targets expose unlisted definition fragments modally. Keyboard/mouse paging and unavailable-content handling are bounded. Its Attack topic includes the corrected Force-inclusive simultaneous rule. | Exact legal-copy decode independently matched at 57,640 normalized source characters and reproduced 80 contexts, 93 internal links with the same 67/26 split, 80 topics, and 73 contents rows; High for readable text, context/contents structure, font-style metadata, link targets, and screen routing; pixel-font rendering is a deliberate cross-platform approximation of native WinHelp typography | Capture native typography/paragraph geometry only if required by visual parity; retain macro/external execution as unsupported for safety |
| Audio | 28 WAV resources and triggers | All WAVs extracted; resolved equipped-weapon attacks retain and route the event-time item Sound index, unarmed attacks route ordinary/Martial-Arts sounds 0/1, detected police route sound 18, and evasion remains silent. Detailed playback emits each cue at its clip's first animation tick, so retaliation waits for the reversed second clip. Historical load/replay events do not replay cues, and elimination cannot erase the event-time equipped sound. The recovered nine-slot general table loads `SND00200`-`SND00208` with its intentional slot-5 gap; every routed panel transition and the idle confirmation use entry/exit slots 0/1 only while Slide Panels is enabled. Slot 2 is the pointer-push cue for full local-setup buttons, all eight main-console control families, Hire Reject, handoff Ready, and endgame controls; successful pointer Hire rejection does not add slot 3. Setup selection/rejection uses slots 3/4, mapped management/command impossibility and rejected-submission paths use slot 4, while accepted submissions and standard panel confirmation/cancellation controls use slot 3 through the original shared helpers. Events/Combat Results/incoming-Comlink paging uses slot 3 for a bounded step and slot 4 at either boundary; changing a populated Combat Results opponent also uses slot 3. Unread Comlink planning entry plays slot 6 immediately and presentation-only cadence repeats it every four seconds until every unread record has been viewed; the planning countdown uses warning slots 7/8. Slot 9 plays at later local turn starts through a direct lower-helper call. General and combat effects share one interrupting playback voice; music is separate. Effects have an independent persisted 0-10 level, default 6, with the exact original amplitude conversion. | Static loader, complete gated-wrapper and slot-2 caller inventories, panel-resource/caller pairing, page-handler branches, accepted-control helpers, Detailed Combat, Options, setup, Combat Results selection, Comlink-recorder, pending-byte/read-bit lifecycle, modal, and timer call graphs plus file/table inspection High; interruption flags and turn cue High, native audible timing Low | Validate slot-6 and slot-9 native cadence, interruption timing, and amplitude |
| Combat animation | Item-selected attack/hit sequences, retaliation, evasion and police attacks | All 98 `PX07xxx` strips decoded as eight 64x64 frames; event-time weapons route normal and mirrored attack/hit pairs into `PX05014`. Static presenter branches recover ordinary unarmed attacker/recipient indices 0/2, Martial-Arts indices 1/18, zero-damage recipient index 1, evasion indices 27/0, and detected-police indices 28/20 (or 28/1 on zero damage); undetected police has no presentation. Handoff auto-presents only visible combat from the immediately completed turn **before** Last Turn Events, matching the original planning-entry call order; each hot-seat player tracks presentation independently and loaded history is suppressed for every viewer. The Detailed preference controls automatic playback, while Combat Summary can replay either mode manually; the recreation retains a bounded/skippable Detailed presentation as an explicit fix for the native freeze. Escape/Cancel clears the queue without exposing or mutating simulation phases. Deterministic twin-resolution coverage proves fully drained Detailed playback leaves the same combat rolls, Force, phase hash, and complete authoritative hash as Simple presentation. | Detailed-combat loader/compositor branches, asset dimensions/frame inspection, item table and original Combat capture High; hot-seat planning-entry order High static evidence; presentation-state isolation tests High; exact cadence/color key Medium | Reproduce the original Detailed freeze; validate cadence and compositing/color key against executable captures |
| Music | Original CD-audio soundtrack, represented by eight GOG Ogg tracks (`Track02`-`Track09`) | Recovered screen programs are wired: title/setup repeats Track 2, gameplay advances Tracks 3-8 then repeats, and endgame repeats Track 9; Options and Help inherit their underlying context without a restart; losing focus pauses and regaining focus resumes; an Options overlay applies the recovered level-5 default and exact 0-10 volume conversion and safely persists the recreation preference; missing or unreadable music disables itself without affecting gameplay | GOG pack and exhaustive 11-call selector plus MCI/aux-volume call graphs, High for track ranges/order/repeat/focus/default-volume and exact menu/program restart boundaries; safe preference persistence is an intentional correction | Validate native playback on Windows, Linux, and macOS |
| Video | Two Smacker v2 movies | Extracted byte-for-byte; bounded managed decoder verifies exact 480x256 dimensions, 200/1,150 frames, 100 ms cadence, mono/stereo packed 22,050 Hz 8-bit audio, complete structural length, palette state, canonical codebooks, and all 1,350 indexed video frames. The first run streams logo then intro at native centered size, queues converted PCM, pauses with focus, consumes explicit skip input, and safely advances after missing-file or playback failure; completing the queue is recorded in preferences so later runs open at the title screen, where an `INTRO` button replays the same queue on demand | Fingerprinted containers, complete source-pack decoding, synthetic malformed-stream/block fixtures, deterministic timeline/policy/stream tests, and a bounded Windows smoke run that completed the logo and started the intro; High for recreation behavior, Medium-Low for unobserved original order/skip and native A/V fidelity | Capture original trigger/skip behavior and validate native visual/audio fidelity on supported platforms |

## Multiple players and networking

| Area | Original requirement | Recreation status | Evidence/confidence | Next parity gate |
|---|---|---|---|---|
| Hot-seat | Multiple humans on one machine | One-to-six local players with original `PX00132` blocking Ready handoff at multi-local active-player transitions; normal handoff is Combat -> Events -> planning; an ongoing eliminated local seat follows `PX00132` -> `PX00203` in slot order before later seats, while completed games use direct shared awards | Multi-local gate, normal presentation order, terminal slot walk, post-elimination gameplay-music selector, and final shared-awards branch High static evidence; recreation records crossed eliminated slots without changing simulation | Capture its full-screen geometry and later-seat timing |
| Legacy network | WinSock/IPX/modem/serial/Mac transports | Explicit non-goal: no port, protocol interoperability, or production exposure | Deliberate security/product exclusion | None; exclusion is final |
| Modern network | Safe deterministic command transport (recreation extension) | Self-hostable sealed-order lockstep with strict contracts, canonical digests, deterministic client resolution, state-hash consensus, host snapshot repair, retrying event streams, and restart reconciliation from the newest verified snapshot plus gaplessly validated ordered event history and the caller's current draft; departures and wholly missed timed turns open unanimous `WAIT`/`USE AI` votes, returning players can reclaim AI-held seats, and replay-v27 records both authoritative controller transfers | Cross-language contract/digest fixtures, server/service tests, HTTP session integration including vote/return/takeover ordering, missing-order human retention and history-gap rejection, and a CI-run two-client real-game smoke path against the Node server; not original parity | Broaden live-runtime recovery scenarios as new failure modes are found |

<!-- The following two superseded City/Sector UI and Input rows are retained as
merge provenance only; the live rows above include the PR's detected-gang
presentation coverage. They are inside this comment so that they do not render.

| City/Sector UI | Original screen/panel workflow | Routed title/setup/handoff/city/sector/gang/finance/ranking/items/Give/Sell/combat-summary/search/commands/hire/events/endgame/help flow starts authoritative recovered cities; setup top portraits and two-by-three player cards use the measured `PX00143` apertures and `PX00140` arrow/name construction, while selection outlines use inset button faces rather than overlapping broad hit rows; queued reports automatically open the paged `PX05010` Last Turn Events panel after handoff using native `PX060xx` illustrations, with completed Research compositing the item's 15-frame `PX04xxx` rotation inside the exact `PX06005` monitor aperture, and Combat Summary uses paged `PX05012` results with native-size sector tiles; Search uses the original `PX05024` site checklist with all 22 per-player filter bytes, exact `PX00150` controlled/selected site markers compacted into three sector slots, and definition-detail double-clicks instead of the former incorrect gang list and sector outlines; the city retains one neutral grid and composites owned-cell interiors without border drift, while whole-city and detailed-sector maps both mark every active-gang sector with exact-white-keyed `PX00129` status art; the upper-right city console shows projected cashflow and hover explanations that distinguish sector Income from passive tax; the shared top bar uses measured portrait cells and the twelve-frame `PX00129` active-player marker; the detailed-sector screen preserves the portrait strip, live right-console routes, 3-by-3 neighborhood, coordinate frames, status overlays and three buildings; Gang Information slides over a stationary City/Sector backdrop, command selection does not slide, nested-detail return does not replay the Sector entrance, disabling Slide Panels cancels every entrance globally, and screen changes clear stale detail-click state; `PX05021`, City Financial, Comlink View, Gangs in Sector, and the three-column `PX05016` Hire comparison use their embedded baselines/apertures and shared panel destination; recreation-only navigation/drag/success hints are suppressed while rejection reasons remain; gang cards have assigned-action strips, stacked repeat marks and green/red Force tracks, stationary double-click opens details, drag-to-neighbor queues Move, and a drop on the gang's own centered sector queues Control; Hire uses the original split price/reject footers and opens `PX05000`; Equip/Research use four category tabs on `PX05004`/`PX05007`, show accumulated/required Research progress, and share `PX05001` Item Information with its exact 48-by-48 aperture and continuous 15-frame item rotation; `PX05002` shows site details from sector/Influence or Search definitions; shared command portraits and Site/Attack subcells use measured template apertures; Influence uses `PX05005`; Attack uses `PX05003`; `PX05013` and `PX05015` provide one-to-three-slot Sell and Give workflows; resolved combat uses the original `PX05014` sector/combatant/equipment/Force layout with recovered animation frames; all mutations are replay-recorded | Static executable evidence High for generator, mapped atlas identities and copy modes, `PX04xxx` dimensions, recovered panel pixels, and Search filter/marker/detail behavior; supplied panel captures High for workflow and Medium for exact hit edges; remaining interaction/coordinates Medium-Low | Add Search and city golden screens |
| Input | Original mouse plus modern keyboard | Letterboxed virtual-coordinate mouse hit testing and keyboard navigation implemented; the recovered Add/Remove/Begin/Cancel setup rectangles retain their pressed identity, draw the exact `PX00140` held-inside tile, restore the baked control outside, act only on release inside the same control, and cancel release outside; setup cards use the exact native selected-card-first gate, 64-by-68 input cells, broad side-arrow and ten-pixel name bands, four-pixel drag box, clamped 40-by-40 token with its keyed native frame, destination selection, and whole-record color move/exchange while retaining separate artwork rectangles; the main city/sector console uses the exact eight native tiles, horizontal paired subcontrols, original-press identity, release-inside cancellation, and opaque `PX00129` pressed sprites; names share a bounded original-font keyboard mapper and enforce the original 10-character cap; city sectors select on single-click and open detail on bounded same-sector double-click; Hire portraits drag as scaled tokens through controlled-sector validation from both city and detailed-sector modes, with a detailed-workspace drop targeting its centered sector; detailed gang portraits use movement-threshold disambiguation so stationary double-click opens details while drag/drop queues only validator-legal targets, a neighboring sector queueing Move and the gang's own centered sector queueing Control; a right-button press edge cancels transient edits, pressed controls, drags, warnings, Detailed Combat playback, and nested panels through their existing close/back paths without held-button repeats; Done presents the recovered optional confirmation when an active gang has no queued command | Complete local-setup and main-console handler/helper paths High; current client and original Hire/sector captures Medium-High; idle-gang Help/static paths High; right-click behavior is recreation integration pending exact original per-screen captures | Remaining original hit maps, exact original per-screen right-click captures, and configurable bindings |
-->

## Computer-player row details

The paragraphs below expand the `AI` row above, one recovered strategy family
at a time.

The outer planner now submits commands in the original 81-slot roster order
(`BIN-AI-003`). When two gangs compete for one affordable Equip or Bribe, the
lower roster slot spends the shared cash budget first even if its reused gang
record has a higher gang ID. Advanced policy expansion and idle recovery use
the same slot order.

The AI row includes all three handler-exact family-1 continuations in the live
planner: previous None/Chaos selects Heal, Move, or Chaos; previous Heal repeats
Heal or selects strict solo Control, then Move; and prior Control/Equip/Snitch
uses the recovered equipment gate before its crime-or-Move continuation.
Replay-recorded preparation uses exact item or mode-5 Move targets. The shared
selector also implements bounded objective modes 12–15, including nearest-ring
search, objective ownership/capacity filters, compounded hostile-human weight,
maximum-tie RNG, and x-then-y routing. The family-13/14 off-objective terminal
Move handlers are live, along with family 14's prior-Control Heal and family-13
transition on an objective. Both families' owned-objective Heal branch is also
live when no visible opponent is present. Their contested-objective parity,
visible-target pools, three-draw retry predicate, exact Attack target, and
Heal/Control fallbacks are live and replay-verified. On-objective weapon/armor
equipment and cooldowns, maximum-Chaos miscellaneous Equip, highest-Support
unfinished-site Influence, and the five-draw owned-visible Attack branch are
also live. Static inspection confirms neither handler assigns Research.
When modern validation cannot project an exact prepared native tuple, Original
policy submits no replacement command; recovered families never fall through
to recreation-scored fallback behavior.

Family 3 now uses its complete recovered handler. Cash-site decisions after
previous None, Control, Equip, Heal, Influence, and Snitch include the
Force-8/effective-Heal-`-3` boundary, first-strict-maximum local Cash site,
previous-site retention, equipment gate/cooldowns, strict solo Control, and
mode-8 movement using summed unfinished positive Cash. After Attack, Hide, or
Move, the visible-opponent branch preserves the original human-pool target and
full-pool comparison-ordinal asymmetry. The terminal path applies the recovered
three-Move family transition and final-three-turn Greed Terminate override.
These decisions, targets, family changes, and RNG draws are replay-recorded.

Family 5 uses the same complete branch order for Support-focused Influence.
Mode 7 sums positive Support across unfinished sites in owned sectors and rejects
a destination when any same-sector gang record has previous Influence. Local
selection keeps the first strict maximum positive-Support unfinished site; the
remaining Heal, Equip, Control, Move, opponent, family-transition, Terminate,
and intentional None paths match the recovered family-3 shape and are live.

Family 9 is live with weapon-before-armor equipment, no existing-cooldown gate,
and cost-times-three replacement cooldowns. Without equipment it uses mode 3
to leave owned territory, attacks a weight-10 visible opponent through the
five-draw asymmetric target/comparison loop, moves after a previous Control,
and otherwise Controls in non-owned territory. Commands, targets, RNG, and
replay behavior are covered.

Family 10 is live with its strict-Defense armor upgrade and literal cooldown 2,
special researched Smoke Bombs Equip, Force-10/no-visible-opponent Heal gate,
and completed-positive-Stealth mode-9 routing. The handler preserves the
original's separate probe and destination calls, including two independent tie
draws, then chooses Chaos or Hide according to same-sector previous Chaos.

Family 11's mode-10 anchors and mode-16 followers use the exact all-81-slot,
every-sixth-family-11 grouping and stored leader destination. Both formation
modes apply unavailable-sector removal and bypass the strict-Control late
filter, which the native selector reserves for planning families 0 and 1.

Family 12 is live with its no-visible-opponent weapon/armor/miscellaneous
priority, raw-cost weapon/armor cooldowns, Force-10 Heal gate, and encoded
current-sector movement. That encoded mode is deliberately cleared by the
shared selector's source exclusion and therefore exercises its all-zero random
tie and one-step route. The visible-opponent branch preserves the human-pool/
full-pool ordinal asymmetry, retries the combat comparison up to five times,
and still attacks its final selected target after five failures. Its exact
targets, RNG consumption, replay behavior, and final-three-turn Greed override
are covered.

## Current blockers to parity claims

- The raw RNG, bounded wrapper, once-per-process low-16-bit uptime seed, all 61
  direct wrapper calls, and accepted-Begin pre-city ordering are statically
  recovered. Native stream correlation still requires the launch seed and
  whether missing `serialNum` consumed two bounded calls during startup.
- City generation and HQ placement match the verified static path, including
  their internal RNG ordering, but still lack an original runtime seed fixture.
  Empty local slots now consume recovered unique portrait/name draws before AI
  initialization and city generation; the complete path still needs an original
  runtime fixture.
- Hire slot selection and refill ordering match the recovered static control
  flow, including action replacement/toggling and resolution-time payment, but
  runtime corroboration and launch-specific RNG correlation remain pending;
  recovered persistent-anchor AI placement is wired while actual placement is
  deferred to the Hire phase.
- The client now drives the authoritative phase coordinator and replay recorder,
  but several original management workflows, hit regions, and visual layers are
  still incomplete or only provisionally mapped.
- Command replacement gets a recreation sequence number for authenticated
  replay, but all six execution subphases use their recovered native scheduler:
  player/roster scans, phase snapshots, grouped sector barriers, board order,
  Control tie draws, and simultaneous Move-capacity normalization as applicable.
- AI Mentality's global, query selector, reaction/attitude state, sector Combat
  + Defense advantage hostility pass, and all nine resolution-band consumers
  are recovered and integrated; family 1's post-equipment equipment/cooldown/
  nearby-danger gate and owner/cash/Mentality/Tolerance continuation are live,
  including exact Equip and Move targets. Every dispatched family handler is
  live; family 1's unavailable-command policy is closed while complete outer-
  policy trace parity remains provisional. Police message edges, equipment,
  objective timing, and audiovisual triggers retain the
  specific parity gaps listed above.

These blockers prevent describing the current playable slice as a faithful
gameplay recreation even though its decoded tables and asset pack are verified.

## Binary evidence status

Initial PE/import/string classification and continuing address-level gameplay
research are recorded in the `original-internals/` documents indexed by
`ORIGINAL-INTERNALS.md`. Verified or high-confidence findings now cover platform
boundaries, source resource paths, save/version strings, the original RNG step
and bounded wrapper, city/site/HQ/hire setup, and the outer AI command
dispatcher. Exact AI planner policy, remaining resolver
ordering/reveal edges, and reference-trace parity retain their explicitly
listed gaps.
