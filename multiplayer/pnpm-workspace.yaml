packages:
  - packages/*
  - runtimes/*
allowBuilds:
  # These packages compile or download the native binaries used by tests and runtime adapters.
  better-sqlite3: true
  esbuild: true
  workerd: true
